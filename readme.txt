My playground for Digilent CmodA7 board.
It uses other IP block as a git repo sub-module.

Actually I am working on a PWM IP and its SDK.
Not all-the-required file are uploaded yet. (but on the way) 
